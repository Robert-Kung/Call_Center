# Gemini Token Server 環境變數範本
# 複製此檔案為 .env 並填入實際值

# 伺服器 port（預設 3005，避免與 Grafana port 3001 衝突）
PORT=3005

# Gemini API Key (真實 key，僅存在此後端，絕不放到前端)
GEMINI_API_KEY=your_real_gemini_api_key_here

# 允許的前端來源 (CORS)
ALLOWED_ORIGIN=http://localhost:3100
